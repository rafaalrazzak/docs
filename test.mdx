---
title: 'New file'
description: 'Description of your new file.'
---

```jsx
function TodoList() {
  const todos = ["Belajar React", "Membaca buku", "Olahraga"];
  
  return (
    <div>
      {todos.map((todo, index) => (
        <div key={index}>{todo}</div>
      ))}
    </div>
  );
}

```